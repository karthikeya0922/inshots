# csvkit-js

Command-line toolkit for slicing, joining and summarising CSV files.

## Features

- **csvcut** — select columns by name or index
- **csvjoin** — join two files on a key
- **csvstat** — summary statistics per column

## Install

```bash
npm install -g csvkit-js
```
